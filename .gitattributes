# This repo is used on Linux/Android/Termux — force LF so shell scripts and
# Python run there regardless of who commits (e.g. from Windows).
* text=auto eol=lf
*.sh text eol=lf
*.py text eol=lf
*.json text eol=lf
